# A3_01e（2026-07-12）数据不足说明

目标测试窗口为 `2026-06-11 00:00:00` 至 `2026-07-12 23:55:00`。
原始点位数据从 `2026-06-18 10:40:00` 到 `2026-07-12 23:55:00`
连续缺失 **7,072** 个 5 分钟槽，超过本任务确认的 288 槽（24 小时）填充上限。

因此本场景：

- 不生成可用于模型测试的 `df_power.csv`；
- `data_process.yaml` 设置为 `enabled: false`；
- LightGBM 配置仅以 `.yaml.disabled` 草案形式保留；
- 补齐原始数据后，需先启用并重新运行数据处理，再恢复模型配置后缀。
